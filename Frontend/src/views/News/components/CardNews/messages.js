/*
 * CardNews Messages
 *
 * This contains all the text for the CardNews component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardNews';

export default defineMessages({
  readArticle: {
    id: `${scope}.readArticle`,
    defaultMessage: 'Read Article',
  },
});
